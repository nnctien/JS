API: Application Programming Interface
Query parameters:
direct to url:....
điều hướng người dùng sang địa chỉ abc.com/search?q=a+b+c&ref=c&author=lyc

trình gỡ lỗi inspect 

get: receive data from server to client
post: send data from client to server 
put: edit all fields of data
patch: edit one fields

form data is not query parameters
	to get data submited: req.body
Mongoose is for working with mongodb in Nodejs

using method-override to edit item(html hasn't supported PUT method yet)

redirect: add a key "location" in response headers

code prettier

We should use single quotes
bucket pattern
Có 7 kiểu nguyên thủy và 1 kiểu tham chiếu
lưu kiểu tham chiếu trong heap

Một API, hay còn gọi là giao diện lập trình ứng dụng , về cơ bản là một cách để các ứng dụng mượn chức năng và dữ liệu của nhau 
Có thể nói bản thân REST là một kiểu mẫu kiến trúc viết API/Web service (architecture pattern). Nó sử dụng phương thức HTTP đơn giản để tạo cho giao tiếp giữa các máy. Vì vậy, thay vì sử dụng một URL cho việc xử lý một số thông tin người dùng, REST gửi một yêu cầu HTTP như GET, POST, DELETE, vv đến một URL để xử lý dữ liệu.
 Javascript engine là một chương trình hoặc trình thông dịch thực thi mã Javascript.
 Switch is faster than if-else. This is due to the compiler's ability to optimise the switch statement